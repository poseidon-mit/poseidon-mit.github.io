// PageShell hero variant tests skipped: PageShell depends on legacy components
// (AppNav, BackButton, Breadcrumb) that are no longer present in this architecture.
import { describe, it } from 'vitest';

describe.skip('Hero variants (legacy PageShell — skipped)', () => {
  it('placeholder', () => {});
});
