// PageShell semantics tests skipped: legacy architecture (AppNav/BackButton/Breadcrumb removed).
import { describe, it } from 'vitest';

describe.skip('PageShell semantics (legacy — skipped)', () => {
  it('placeholder', () => {});
});
